#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
char c;
scanf("%c",&c);
printf("%c\n",c);
char s[30];
scanf("%s",s);
printf("%s\n",s);
char sen[400];
getchar();
gets(sen);
printf("%s",sen);

return 0;
}
